{
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/tool-cache": "^2.0.1",
    "@types/node": "^20.3.1",
    "esbuild": "^0.18.2",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "esbuild ./src/setup.mjs --bundle --outfile=dist/setup.js --platform=node --minify"
  }
}
